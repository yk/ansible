to run a playbook
ansible-playbook playbooks/[playbook-name].yml

optionally --ask-pass if you don't have ssh key set up and --ask-sudo-pass if you don't have nopasswd sudo

the ssh-config file is an example of what you should have in your ssh config, modify the username and host names to your liking
